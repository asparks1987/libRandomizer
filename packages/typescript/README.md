# @librandomizer/typescript

```ts
import { randomInt, randomFloat, randomChar } from "@librandomizer/typescript";

const value = randomInt();
const ratio = randomFloat(0, 1);
const letter = randomChar("A", "Z");
```
