# @librandomizer/javascript

```js
const { randomInt, randomFloat, randomChar } = require("@librandomizer/javascript");

const value = randomInt();
const ratio = randomFloat(0, 1);
const letter = randomChar("A", "Z");
```
